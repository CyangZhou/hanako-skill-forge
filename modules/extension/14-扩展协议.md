# 14-扩展协议.md

## 可扩展对象

- 新路由；
- 新模块；
- 新模板；
- 新辅助脚本；
- 新结构化契约；
- 新参考资料目录。

## 扩展顺序

1. 先确认当前缺的是什么；
2. 再确认是否能用文档解决；
3. 文档不够时，再加脚本；
4. 新增后要登记落点、边界、验收方式；
5. 扩展后必须回到维护和验收链检查冲突。

## 扩展规则

- 一次扩展只补一个明确能力；
- 新模块不能和旧模块职责打架；
- 新脚本必须有明确输入输出；
- 扩展后必须可被路由命中；
- 扩展能力要写入能力注入清单。
